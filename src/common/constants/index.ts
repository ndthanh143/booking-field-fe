import { LocationMap } from '@/services/venue/venue.dto';

export const DEFAULT_MIN_PRICE = 0;
export const DEFAULT_MAX_PRICE = 500000;
export const RATING_PAGE_LIMIT = 0;
export const DEFAULT_NOTIFICATION_PAGE = 1;
export const DEFAULT_NOTIFICATION_LIMIT = 10;
export const defaultLocation: LocationMap = {
  lat: 10.0652525,
  lng: 106.245213,
};
